# QA_REPORT.md — Сквозной аудит портала MathPulse

> **Этап 6 из 7.** Финальная проверка после объединения 8 проектов и подключения
> сквозных сервисов. Дата: 2026-06-14.

---

## 0. Итог

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit` | ✅ 0 ошибок |
| `npm run lint` | ✅ 0 ошибок (9 warnings — fast-refresh в shadcn/ui, допустимо) |
| `npm run build` | ✅ успешно, sitemap.xml (75 URL) сгенерирован |
| `npm run test` | ✅ 13/13 (App + 11 страниц тем + SectionHub) |
| Целостность внутренних ссылок | ✅ 0 битых (65 готовых тем, 9 хабов, кросс-ссылки) |

---

## 1. Целостность маршрутов и ссылок

Автоматическая сверка (скрипт по `topicMap` + `App.tsx`):

- **Каждая `ready`-тема** имеет рабочий маршрут: `topicHref(topic)` (route ?? slug)
  совпадает с одним из `<Route>` в `App.tsx`. Расхождений — **0**.
- **Каждый хаб раздела** (`/functions` … `/statistics`) обслуживается маршрутом
  `/:sectionId` → `SectionHub`. Расхождений — **0**.
- **Все id в `crosslinks.ts`** существуют в `topicMap`. Битых — **0**.
- **Дубли маршрутов** в `App.tsx` — **0**.

Всего маршрутов: 90 (38 уроков host, 8 справочных, 6 интегралов, 4 производных,
2 предела, 5 рядов, 4 дифура, 6 лин. алгебры, 6 вероятность, 5 статистика,
2 визуализации, хабы, гид, редиректы).

## 2. Редиректы со старых URL

Старые пути объединённых проектов вели на 404. Добавлены редиректы (`Navigate`):

| Старый URL | Назначение |
|---|---|
| `/section/:id` (strang, `_-_`) | `→ /` |
| `/tickets` (remix-of) | `→ /` |
| `/notes` (`_-_`) | `→ /` |
| `/progress` (`_-_`) | `→ /` |

Внутренних ссылок на эти пути в `src/` не осталось (проверено grep).

## 3. Рендер страниц (runtime)

Добавлен `src/test/Pages.test.tsx`: монтирование 10 новых страниц тем в
`MemoryRouter` + `SectionHub`. Проверяет отсутствие рантайм-ошибок (включая
авто-рендер KaTeX в `TexContent`). Все проходят.

## 4. Исправленные дефекты

| Файл | Проблема | Исправление |
|---|---|---|
| `eslint.config.js` | линтовался `new_materials/` (40+ ложных ошибок) | добавлен в `ignores` |
| `eslint.config.js` | shadcn `ui/*` и `tailwind.config.ts` ломали lint | overrides (как в прошлом аудите) |
| `src/components/diffeq/DifferentialEquationChart.tsx` | `no-case-declarations` | блок `case` обёрнут в `{}` |
| `index.html` | Lovable-плейсхолдеры, `lang="en"` | `lang="ru"`, осмысленные мета/OG, favicon |

## 5. Открытые вопросы / отложено

1. **Домен sitemap.** База — `https://calculus-visuals.vercel.app` (env `SITE_URL`).
   Подтвердить прод-домен.
2. **Страницы `/search`, `/progress`** из `SITE_ARCHITECTURE` — пока редирект на `/`
   (поиск реализован палитрой ⌘K; отдельная страница прогресса не создавалась).
3. **Браузерная проверка** интерактивных графиков (поля направлений, фазовые
   портреты, heatmap корреляций) — за рамками статического QA, рекомендуется вручную.
4. **Старый `AUDIT_REPORT.md`** относится к доменно-независимому аудиту host до
   объединения; этот отчёт — про объединённый портал.
